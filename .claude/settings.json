{
  "permissions": {
    "allow": [
      "Bash(flutter clean *)",
      "Bash(python3 -)",
      "Bash(dart format *)",
      "Bash(grep -rn \"color: Colors.white,$\" lib/screens --include=\"*.dart\")",
      "Bash(awk -F: '{s+=$2} END {print s}')"
    ]
  }
}
